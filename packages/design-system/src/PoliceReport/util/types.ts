/**
 * PoliceReport Types
 * Auto-generated from Storybook component
 */

export interface PoliceReportConfig {
  /** Enable Focus Mode - allow component to expand as primary interaction surface */
  focusable?: boolean;
  /** Focus Mode Label - name shown in chat input when this component is focused */
  focusLabel?: string;
  object?: object;
}

export interface PoliceReportOutput {
  __outputs: {
    componentSpec: any;
  };
}

export interface PoliceReportTemplate {
  componentUrl: string;
}
