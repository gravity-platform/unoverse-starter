{
  "name": "Bank Transfer",
  "description": "A guided flow for sending money — beneficiary, amount, review, confirm.",
  "whenToUse": "Transfer or send money — pay a beneficiary or move funds between accounts through a guided choose-review-confirm flow.",
  "category": "Payments",
  "version": "1.0.0",
  "defaultState": "focus",
  "inputSchema": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "The user's request"
      }
    }
  },
  "binding": {
    "workflow": "wf-r4jzo7",
    "trigger": "inputtrigger6"
  },
  "autoTrigger": true,
  "expose": {
    "openaiApps": false
  },
  "previewComponents": [
    "AccountTransferWidget"
  ]
}
