import type { GravityTemplateProps } from "../core";

/**
 * BasicLayout Props
 * Minimal template - just renders components vertically
 */
export interface BasicLayoutProps extends GravityTemplateProps {
  autoScroll?: boolean;
}
